// ── Brand (static codebase identity — not fetched at runtime) ──
/** One word. Every other string in the product writes "DateRoom" — the
 *  spaced form here was the only place it split, and it showed on the room's
 *  own wordmark. */
export const BRAND_NAME = "DateRoom";
export const BRAND_TAGLINE = "A little space, just for us";
export const BRAND_DESCRIPTION = "Structured virtual dates that spark real conversation.";

export type {
  ActivityTabId,
  PricingPlanKey,
} from "@/lib/appConfigSeed";
